package ai.classifai.controller.image;

import io.vertx.core.Vertx;

public class PolygonAnnotationController extends ImageAnnotationController
{
    public PolygonAnnotationController(Vertx vertx)
    {
        super(vertx);
    }
}
